---
id: terminalForm

load_config_file:
  - backoffice/airport_popup.yml

elements:
  - type: Text
    name: name
    label: Терминал
    filters:
      - TrimEdges
    constraints:
      - Required

  - type: Submit
    name: submit
